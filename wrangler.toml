name = "vsteike"
compatibility_date = "2024-01-01"

# Cloudflare Pages Configuration
# Pages deployments don't use most wrangler.toml settings
# This file is minimal and mainly for local development with wrangler pages dev

# For production deployments, configure settings in the Cloudflare Dashboard
# under Pages > Project Settings
